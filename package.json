{
  "name": "fair_backgammon",
  "private": true,
  "scripts": {
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm --prefix web install --include=dev && npm --prefix web run build"
  },
  "engines": {
    "node": ">=18"
  }
}
