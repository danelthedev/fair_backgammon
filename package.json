{
  "name": "fair_backgammon",
  "private": true,
  "scripts": {
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm --prefix web install --include=dev && npm --prefix web run build && go build -o app"
  },
  "engines": {
    "node": ">=18"
  }
}
